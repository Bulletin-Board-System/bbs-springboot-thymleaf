server:
  port: 8088
spring:
  application:
    name: bbs
  datasource:
    driver-class-name=com: mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/bbs?useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&autoReconnect=true&useSSL=false&allowMultiQueries=true
    username: root
    password: admin123
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
mybatis-plus:
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl